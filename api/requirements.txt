# ═══════════════════════════════════════════════
# CryptoAI Master — API Server Dependencies
# ═══════════════════════════════════════════════
# pip install -r api/requirements.txt

# ── 코어 ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── CORS / 환경변수 ──
python-dotenv>=1.0.0

# ── 데이터베이스 ──
psycopg2-binary>=2.9.9
# supabase>=2.3.0          # 선택사항

# ── Redis (선택) ──
# redis>=5.0.0

# ── HTTP 클라이언트 ──
httpx>=0.25.0

# ── 로깅 ──
loguru>=0.7.0

# ── WebSocket ──
python-socketio>=5.10.0

# ── 거래소 API ──
pyupbit>=0.2.33

# ── 인증 (선택) ──
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4
